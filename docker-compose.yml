services:
  github-runner-base:
    image: ghcr.io/rafal-lukawski/github-runner-base:latest
    build:
      context: .
      dockerfile: Dockerfile.base
      platforms:
        - linux/amd64
        - linux/arm64
  github-runner:
    image: ghcr.io/rafal-lukawski/github-runner:latest
    build:
      context: .
      dockerfile: Dockerfile
      platforms:
        - linux/amd64
        - linux/arm64

